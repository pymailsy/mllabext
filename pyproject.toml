[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "mllabext"
version = "0.0.1"
description = "Run ML Lab examples from GitHub"
requires-python = ">=3.8"
dependencies = ["requests"]

[tool.setuptools]
packages = ["mllabext"]
